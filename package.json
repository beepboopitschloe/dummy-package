{
  "name": "dummy-package",
  "version": "1.0.0",
  "description": "Empty package for testing yarn installs with git",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
